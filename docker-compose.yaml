version: "3.9"
services:
  agentic-mcp:
    build: .
    container_name: agentic_gamedev_mcp
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
      - NODE_ENV=development
    command: npm run validate
